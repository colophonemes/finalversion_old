.row
	.col-sm-12
		h1.pagetitle Start working on...
		ul.tasks.tasks-run
			li.task( ng-repeat="task in tasks | filter : {selected : true} | reverse",ng-class="{current : $first}")
				span.index {{$index+1}}.  
				| {{task.name}}
				span(ng-show="$first")
					button.btn.btn-default.btn-xs(ng-click="updateTask(task._id,{selected:false, status:'incomplete', date: 'now'})") Done working 
						span.fa.fa-caret-square-o-right
						| 
					button.btn.btn-success.btn-xs(ng-click="updateTask(task._id,{selected:false, status:'complete'})") Completed 
						span.fa.fa-check-square
.row
	.col-sm-12
		a.btn.btn-default(href="/tasks/select" ng-show="tasksSelectedCount > 0") Back
		a.btn.btn-success(href="/tasks" ng-show="tasksSelectedCount == 0") All done!